#include <iostream>
using namespace std;

int main() {
	int x;
	float y,bal;
	cin>>x>>y;
	if(0 < x <= 2000 && 0 < y <= 2000)
	{
	    if(x%5==0 && (x+0.5)<y)
	      bal=y-x-0.5;
	    else
	      bal=y;
	}
	
	cout<<bal;
	return 0;
}
