#include <bits/stdc++.h>
using namespace std;

int main() {
	int t,a[3];
	int sum=0,count=0;

	cin>>t;
	while(t-->0)
	{
		for(int i=0;i<3;i++)
			cin>>a[i];
		sum=0;
		for(int i=0;i<3;i++)
			if(a[i]==1) sum++;
		if(sum>=2)
			count++;
		cout<<count;
	}
	
}

